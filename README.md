## Run:

Install packages:

```
yarn
```

Run the two commands in two separate terminals:

```
yarn watch:css
```

```
yarn watch:html
```

NOTE: do not edit the `main.css` file (it's auto generated)

## Build

```
yarn build
```

The resulting build is in the `dist` folder.
